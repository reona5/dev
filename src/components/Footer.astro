---
import { Image } from 'astro:assets'

const headerItems = [
  {
    name: 'RSS',
    href: '/rss.xml',
    imagePath: '/assets/rss-icon.png',
  },
  {
    name: 'Twitter',
    href: 'https://twitter.com/reona_5',
    imagePath: '/assets/twitter-icon.png',
  },
  {
    name: 'GitHub',
    href: 'https://github.com/reona5',
    imagePath: '/assets/github-black-icon.png',
  },
]
const today = new Date()
---

<footer class="flex justify-between max-w-4xl m-auto p-8">
  <p>
    &copy; {today.getFullYear()} Reona Shimada.
  </p>
  <nav>
    <ul class="flex p-0 gap-4">
      {
        headerItems.map(item => (
          <li>
            <a href={item.href} target="_blank">
              {item.imagePath ? (
                <Image
                  src={item.imagePath}
                  alt={`${item.name} icon`}
                  width={24}
                  height={24}
                />
              ) : (
                item.name
              )}
            </a>
          </li>
        ))
      }
    </ul>
  </nav>
</footer>
